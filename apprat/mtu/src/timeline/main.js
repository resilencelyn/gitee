import { define } from '../../core.js'

const template = `<style>:host{display:block;padding:16px;border:solid 1px var(--color-border);border-radius:2px}</style><slot></slot>`

define('timeline', { template })
